services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: laravel-cluster-4
    restart: unless-stopped
    working_dir: /var/www/html
    ports:
      - "9094:80"
    env_file:
      - .env
